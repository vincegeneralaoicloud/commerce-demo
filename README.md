# commerce-demo
commerce-demo

This is purely frontend code using quasar (vuejs).
The response json is given and is being consumed directly.

Install quasar cli as per https://quasar.dev/start/quasar-cli

Do npm install.

To execute in developer mode: run 'quasar dev' at terminal
